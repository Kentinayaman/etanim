def run_simulation(parameters):
    # Perform some operations to simulate a process
    simulation = {
        'success': True,
        'data': [1, 2, 3, 4]
    }
    
    # Return the simulation result
    return simulation

# Calling the function
result = run_simulation(some_parameters)
print(result)  # Output: {'success': True, 'data': [1, 2, 3, 4]}
